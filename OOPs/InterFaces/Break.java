package InterFaces;

public interface Break {
    // Note: Method names in Java should start with a lowercase letter by convention (e.g., 'brake()').
    // This defines a method for applying the brake.
    void Break();

    // This interface also has a start() method. A class can implement multiple interfaces,
    // even if they have methods with the same name.
    void start();
}