package InterFaces;

// This is another interface, defining the contract for any media player.
public interface Media {
    // A media player must have a way to start.
    void start();
    // A media player must have a way to stop.
    void stop();
}